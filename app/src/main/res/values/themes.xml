<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SchoolClubsManagementSystem" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/red</item>

    </style>

    <style name="Theme.SchoolClubsManagementSystem" parent="Base.Theme.SchoolClubsManagementSystem" />

    <!-- Define a custom theme -->
    <style name="AppTheme.NoActionBar.Fullscreen" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="Theme.Nav.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Nav.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>